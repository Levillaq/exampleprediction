# 🔧 Пример файла переменных окружения
# Скопируйте этот файл в .env и заполните реальными значениями:
# cp env.example .env

# === ОБЯЗАТЕЛЬНЫЕ НАСТРОЙКИ ===

# Токен Telegram бота (получите у @BotFather)
TELEGRAM_BOT_TOKEN=123456789:YOUR_BOT_TOKEN_HERE

# === НАСТРОЙКИ РАЗРАБОТКИ ===

# Секретный ключ для подписи токенов (измените в продакшене!)
SECRET_KEY=your-super-secret-key-change-this-in-production

# База данных (для локальной разработки)
DATABASE_URL=postgresql://user:password@localhost:5432/prediction_bot

# API настройки
API_HOST=0.0.0.0
API_PORT=8000

# Frontend URL для API запросов
REACT_APP_API_URL=http://localhost:8000

# URL API сервера для бота
API_BASE_URL=http://localhost:8000

# URL веб-приложения для кнопки в боте
WEBAPP_URL=http://localhost:3000

# === ПРОДАКШН НАСТРОЙКИ (заполните после деплоя) ===

# URL вашего backend API (после деплоя на Railway/Render)
# BACKEND_URL=https://your-backend.railway.app

# Firebase Project ID (после создания проекта)
# FIREBASE_PROJECT_ID=prediction-bot-12345

# === СЕКРЕТЫ ДЛЯ GITHUB ACTIONS ===
# Эти секреты добавляются в GitHub → Settings → Secrets

# Railway токен (получить в Railway Dashboard → Account → Tokens)
# RAILWAY_TOKEN=your_railway_token

# Render настройки (получить в Render Dashboard)
# RENDER_SERVICE_ID=your_service_id
# RENDER_API_KEY=your_api_key

# Firebase токен (получить командой: firebase login:ci)
# FIREBASE_TOKEN=your_firebase_token

# Firebase Service Account JSON (альтернатива токену)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# === БЫСТРЫЙ СТАРТ ===
# 1. Создайте бота у @BotFather и получите токен
# 2. Скопируйте файл: cp env.example .env
# 3. Вставьте токен в TELEGRAM_BOT_TOKEN
# 4. Запустите: ./deploy.sh all 